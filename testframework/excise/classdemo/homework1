作业1
1.设计一个表示服务器的类。包含服务器的属性有：
    cpu个数，
    内存大小，
    磁盘空间大小，
    操作系统类型（Linux，Windows）
    其中操作系统类型设置为私有变量，外部不可以更改。实现一个方法，输出服务器的属性内容为一下格式：
    8核CPU 40G内存，150G磁盘空间，Linux
作业2
1.设计一个作业1中服务器的子类，实现一个新的方法，根据CPU个数，内存大小，磁盘空间大小计算出服务器当前
价格在子类里面实现一个服务器价格的函数。
计算公式：CPU个数*1527.679+内存大小G*100.21+磁盘空间大小G*50.789.返回数据类型为浮点型。保留
小数点后2位（round方法实现小数点后2位）。并实例化此方法，打印出价格。
